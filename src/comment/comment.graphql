type Comment {
  id: String!
  name: String!
  title: String
  content: String
  item: Item
}

type Item {
  id: String!
  name: String
  status: String
}

type Query {
  comments: [Comment]
  comment(id: String): Comment
}
